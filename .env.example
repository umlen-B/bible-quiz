# Google Analytics 4 measurement id. Copy to .env.local for local runs, and set
# the same value as a repository variable named VITE_GA_ID for the deploy.
# Leave unset and analytics is compiled out entirely — nothing is loaded or sent.
VITE_GA_ID=G-XXXXXXXXXX
