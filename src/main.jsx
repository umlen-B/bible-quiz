import React from "react";
import ReactDOM from "react-dom/client";
import MarkQuiz from "./MarkQuiz.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MarkQuiz />
  </React.StrictMode>
);
